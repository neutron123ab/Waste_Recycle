import { Module } from '../../../types/types';
export declare function createSwiftResolveTypeReferenceName(namespace: string, ast: Module): (name: string) => string;
export declare function parseSwiftPackage(filename: string): {
    package: string;
    namespace: string;
    class: string;
};
export declare function compileSwift(filename: string): Promise<void>;
